{% extends 'base.html.twig' %}

{% block body %}
    <h3>Centre creation</h3>

    {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('centre_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
